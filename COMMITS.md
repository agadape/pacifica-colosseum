# Pacifica Colosseum — Commit Rollback Reference

| Commit | Layer | Date | Files | What Works | Known Issues |
|--------|-------|------|-------|------------|--------------|
| `ea22a1d` | 0-4 | 2026-03-27 | 57 | Next.js dev server (port 3000), Engine (port 4000), Supabase connected (11 tables, 13 badges), Pacifica SDK (REST + WS, signing verified), Privy auth (JWT middleware, auto-register), Arena API (create/list/detail/join/leave), Arena timer (scheduled starts) | Pacifica beta code needed for live trading. Vercel/Railway deploy not done (tasks 0.9/0.10). |
| `a178114` | 5 | 2026-03-27 | +10 | All of above + order validator (round rules), order relay (Pacifica execution), trade/cancel/positions/orders API routes, engine internal endpoints with auth | Same as above. Engine needs `DOTENV_CONFIG_PATH=.env.local` to load env vars when run standalone. |
| `ce3858a` | 6 | 2026-03-27 | +5, ~3 | All of above + real-time price feed (WS), per-tick equity/drawdown calc, elimination/Second Life logic, 30s periodic sync + equity snapshots, 3s leaderboard updates. Full engine pipeline: arena start → risk monitor → trade updates → drawdown detection. | Same as above. |
| `7b1641c` | 7 | 2026-03-27 | +6, ~1 | All of above + complete game loop: round progression (advanceRound), grace periods (2min pause + baseline reset), 3 elimination types (ranking/inactivity/leverage), settlement (close positions, return funds, winner + badges). Loot calc stubbed. | Loot stub needs Layer 8. Sudden Death safety valve noted but not fully tested. |
| `043204e` | 8 | 2026-03-27 | +1, ~1 | **FULL BACKEND COMPLETE.** All of above + loot calculator (Wide Zone + Second Life). Complete game loop: arena start → rounds → eliminations → loots → settlement → winner. | Pacifica beta code still needed for live trades. Frontend not started. |
| `00cac28` | 9 | 2026-03-27 | +13, ~3 | All of above + full frontend shell: light theme, cinematic landing page, arena list/create/detail, profile, leaderboard. Navbar, ArenaCard, Timer, RoundIndicator. TanStack Query hooks, Zustand store. All pages 200 OK. | Profile + leaderboard are placeholders. Trading UI not built yet (Layer 10). |
| `c3c0f54` | 10 | 2026-03-27 | +12 | All of above + full trading UI: order form (market/limit, leverage), chart (Lightweight Charts v5 + WS), positions (real-time PnL), orders (cancel), account panel (drawdown meter + loots), background effects (gradient orbs + particles). | AccountPanel shows placeholder equity ($1000). Needs live data integration. |
| `4fd8cb8` | 11 | 2026-03-27 | +11 | **ALL MUST+SHOULD HAVE COMPLETE.** All of above + spectator view: survivor grid (animated reorder), activity feed (11 event types), elimination banner (red, 5s dismiss), voting (1/wallet), round transition (countdown overlay), status badges. Vote + events API. | Sound effects skipped (Layer 15). Real-time via polling not Supabase Realtime yet (Layer 12). |
| `b8d2e1a` | 12 | 2026-03-27 | +2, ~3 | All of above + Supabase Realtime (5 channels), useArenaRealtime hook, WS exponential backoff reconnect. | Same as above. |
| `fc2bf84` | 13 | 2026-03-27 | +5, ~2 | All of above + Fuul sybil check (stubbed), Elfa AI sentiment + commentary (live), MarketContext component, referral link on profile. | Fuul API key pending invitation. |
| `ee52601` | 14 | 2026-03-27 | +5, ~1 | All of above + DEMO_MODE: mock prices (random walk), mock Pacifica (in-memory), 6 bot traders, auto-setup Blitz arena. Full demo without Pacifica dependency. | Layer 15 (polish + deploy) remaining. |
| `d16f236` | 15 | 2026-03-27 | ~5 | All of above + 3D models (Three.js sword + trophy), dark hero with arena background, shimmer gradient text, transparent navbar, animated counters. | Pacifica testnet access needed. |
| `69bcf69`…`d921ba1` | 15 | 2026-03-30–04-07 | ~14 | All of above + full demo trader flow: mock orders in-memory, real user subaccounts, live AccountPanel (equity/drawdown), positions PnL, trade history panel, USD notional size fix, trader UX polish (elimination overlay, drawdown warnings, round toasts), zombie arena recovery (resume-instead-of-reset, watchdog every 30s). | Supabase flooding from engine (3s timers × new client per call = 113 req/min spike). |
| `290eba4`…`b1491b1` | 15 | 2026-04-08 | ~8 | All of above + Supabase singleton (engine/src/db.ts shared by all 14 services), pnlTimer 3s→10s + Promise.all parallel (10× fewer DB requests), snapshotTimer 5s→30s, system user retry logic (unique referral_code fallback), arenas GET fixed (remove bad join, anon key fallback, error logging), winner status fix, live real-user PnL in leaderboard, winner overlay on trade page, completed arenas hidden from default view. | Zombie arenas still exist in DB (completed status, hidden from UI). Demo day delivery mode. |
| `7d2ee1a`…`aa7182a` | 15 | 2026-04-09 | ~11 | All of above + full Supabase stability: maybeSingle()+limit(1)+order on arena existence checks (was .single() → null on multiple rows → new arena every 30s), mutex guard on setupDemoArena/setupTraderDemoArena (race condition fixed), waitForSupabase() at engine startup (pings every 5s before heavy setup), watchdog 30s→60s, startup stagger (5s gap), pnlTimer 10s→15s + busy-guard, snapshotTimer 5s→30s on trader arena, frontend hooks 3-5s polling → 60s fallback (85% fewer client requests). DB stable: 1 Demo Arena + 1 Open Arena, no zombies. | Demo day submission tasks remaining (15.21, 15.23–15.25). |
| `e86c9b1` | M-2+M-4 | 2026-04-09 | +26 | All of above + Ability System (shield/fortress/second_wind/sabotage awarded per round, activated mid-round, hot-path cached in TraderState) + Hazard Events (7 types, weighted random scheduling 1-3/round, warning→active→expired lifecycle, enforced via ArenaState in-memory). HazardBanner fixed-top on trade+spectate. Both tsc clean. | Enable Realtime on active_hazard_events in Supabase dashboard for live push. |
