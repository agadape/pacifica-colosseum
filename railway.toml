[build]
builder = "NIXPACKS"
buildCommand = "cd engine && npm install && npm run build"

[deploy]
startCommand = "cd engine && node dist/engine/src/index.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
