[build]
builder = "NIXPACKS"
buildCommand = "cd engine && npm install && npm run build"

[deploy]
startCommand = "node -e \"process.stdout.write('engine paused\\n'); setTimeout(()=>{}, 2147483647)\""
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
