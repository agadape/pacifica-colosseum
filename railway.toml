[build]
builder = "NIXPACKS"
buildCommand = "cd engine && npm install && npm run build"

[deploy]
startCommand = "node -e \"setTimeout(()=>{},2147483647)\""
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
